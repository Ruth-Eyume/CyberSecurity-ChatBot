<Window x:Class="CyberBotGUI.TaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Cybersecurity Task"
        Height="360"
        Width="420"
        WindowStartupLocation="CenterOwner"
        Background="#1F0F18"
        ResizeMode="NoResize">

    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock
            Text="Task Title"
            Foreground="White"
            FontWeight="Bold"/>

        <TextBox
            x:Name="txtTitle"
            Grid.Row="1"
            Height="35"
            Margin="0,5,0,15"/>

        <TextBlock
            Grid.Row="2"
            Text="Description"
            Foreground="White"
            FontWeight="Bold"/>

        <TextBox
            x:Name="txtDescription"
            Grid.Row="3"
            Height="60"
            AcceptsReturn="True"/>

        <StackPanel
            Grid.Row="4"
            Margin="0,20,0,0">

            <TextBlock
                Text="Reminder Date"
                Foreground="White"/>

            <DatePicker
                x:Name="dpReminder"
                Margin="0,5"/>

            <Button
                Content="Save Task"
                Height="40"
                Margin="0,15"
                Background="#FF4FA3"
                Foreground="White"
                Click="BtnSave_Click"/>

        </StackPanel>

    </Grid>

</Window>
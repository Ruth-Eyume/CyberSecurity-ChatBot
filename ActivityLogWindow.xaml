<Window x:Class="CyberBotGUI.ActivityLogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Activity Log"
        Height="450"
        Width="400"
        Background="#1F0F18"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- TITLE -->
        <TextBlock Text="ACTIVITY LOG"
                   Foreground="#FF4FA3"
                   FontSize="22"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,10"/>

        <!-- LOG LIST -->
        <ListBox x:Name="lstLog"
                 Grid.Row="1"
                 Background="#2A141F"
                 Foreground="White"
                 BorderThickness="0"
                 FontSize="14"/>

        <!-- BUTTONS -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,10">

            <Button Content="Refresh"
                    Width="120"
                    Margin="5"
                    Background="#FF4FA3"
                    Foreground="White"
                    Click="BtnRefresh_Click"/>

            <Button Content="Close"
                    Width="120"
                    Margin="5"
                    Background="#FF4FA3"
                    Foreground="White"
                    Click="BtnClose_Click"/>

        </StackPanel>

    </Grid>
</Window>